This project is about creating, inserting, searching and deletenig nodes oa a height balanced tree. When inserting and deleting the program must check if the tree is yet balanced. 
If its not, then it does the required rotations so the tree can continue to be height balanced.
